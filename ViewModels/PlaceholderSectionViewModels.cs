namespace AvaloniaApplication1.ViewModels;

// Placeholder sections — these are the next two milestones.
// Kept as real ViewModels so navigation, DI and the ViewLocator
// don't need to change when the actual implementations land.

public partial class ServersSectionViewModel : ViewModelBase
{
    // TODO: GET api/v1/servers (PaginatedServersListDTO), POST api/v1/servers,
    //       then per-server: channels, participants, roles.
}